# MorningStar v0.1 does not require API keys.
#
# Keep this file as documentation only. Put private values in `.env`, which is
# ignored by git.

# Optional future integrations:
# OPENAI_API_KEY=
# DEEPSEEK_API_KEY=
